/**
  ******************************************************************************
  * @file           : debug_uart.h
  * @brief          : 调试串口功能接口头文件
  * @author         : 自定义（可补充）
  * @date           : 2026-01-23
  * @version        : V1.0
  ******************************************************************************
  * @attention
  *
  * 本文件提供调试串口的核心接口，支持 ASCII 格式数据打印（用于串口调试助手）
  * 和 VOFA+ 伪字节流图形化格式数据发送（用于波形图显示）。基于 STM32 HAL 库。
  *
  * 1. 使用前请确保已初始化对应的 UART 外设（如 huart6）并设置正确的波特率；
  * 2. VOFA+ 格式数据需要配合 JustFloat 协议使用；
  * 3. 若通信异常请通过宏定义检查缓冲区容量。
  *
  ******************************************************************************
  */

#ifndef __DEBUG_UART_H
#define __DEBUG_UART_H

//....此处替换串口号路径........只要改一个路径就行
#include "usart.h"
//...................................
#include <stdio.h>
#include "stdint.h"
#include <string.h>
#include <stdarg.h>

void Vofa_JustFloat(float *_data, uint8_t _num);

#endif /* __DEBUG_UART_H */
